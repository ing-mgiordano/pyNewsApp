

const Noticia = ({noticia}) => {

    /* console.log(noticia) */

    const {urlToImage, url, title, description, source} = noticia

    return (
        <div>
        
        </div>
    )
}

export default Noticia
